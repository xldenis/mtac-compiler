(library
  (name mtaclite)
  (synopsis "MTAC-lite plugin")
  (flags :standard)
  (libraries coq.plugins.ltac)
  (public_name mtaclite.plugin))

  (rule
    (targets mtaclite.ml)
    (deps (:pp-file mtaclite.mlg))
    (action (run coqpp %{pp-file})))

